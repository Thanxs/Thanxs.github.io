'use strict';

function Car(name = 'Model unknown', year = 'Year of issue unknown', color = 'Color uncknown', price = 'Price unknown') {
    this.name = name;
    this.year = year;
    this.color = color;
    this.price = price;
}